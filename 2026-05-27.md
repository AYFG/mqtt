# MQTT + Spring Boot 실습 기록

## 프로젝트 세팅

### 의존성
- Spring Boot 3.5.14, Java 17
- spring-integration-mqtt, spring-boot-starter-web
- spring-boot-starter-data-jpa, H2 (인메모리 DB)
- Lombok, spring-boot-docker-compose
- spring-boot-starter-actuator
- org.eclipse.paho:org.eclipse.paho.client.mqttv3 (Paho 수동 추가 필요)

### 파일 구조
```
src/main/java/com/example/mqtt/
├── MqttApplication.java
├── MqttConfig.java        # 브로커 연결, Subscriber/Publisher
├── MqttController.java    # REST API
└── MqttGateway.java       # Outbound 채널 인터페이스
```

---

## Step 1: Spring Boot 프로젝트 생성

Spring Initializr로 생성. spring-integration-mqtt는 검색에 안 나와서 수동 추가.

---

## Step 2: EMQX Docker 세팅

```yaml
# compose.yaml
services:
  emqx:
    image: emqx/emqx:latest
    ports:
      - "1883:1883"     # MQTT
      - "18083:18083"   # 대시보드
```

- `spring-boot-docker-compose` 의존성 덕분에 `bootRun`만 하면 EMQX 자동 실행
- 대시보드: `localhost:18083` (admin/public)

---

## Step 3: H2 인메모리 DB 추가

JPA 의존성이 있어서 DataSource 필요 → `runtimeOnly 'com.h2database:h2'` 추가

---

## Step 4: 포트 충돌 해결

8080이 이미 사용 중 → `server.port=9092` 로 변경

---

## Step 5: MQTT Config

- `MqttPahoClientFactory` → 브로커 연결
- `MqttPahoMessageDrivenChannelAdapter` → Inbound (구독)
- `MqttPahoMessageHandler` → Outbound (발행)
- `DirectChannel` → In/Out 채널

---

## Step 6: MQTT Controller + Gateway

- `POST /mqtt/publish?topic=...&message=...`
- `MqttGateway` 인터페이스로 outbound 채널과 연결

---

## Step 7: EMQX가 꺼지는 문제

Spring Boot 종료 시 Docker Compose도 같이 내려감 → `docker start emqx`로 수동 재시작

---

## Step 8: MQTT Pub/Sub 테스트

### Public 브로커 실험
```yaml
broker: tcp://broker.emqx.io:1883  # 공용 무료 브로커
```

공용 브로커에서 `sensor/data`로 남의 데이터가 들어오는 현상 발견:
```json
{"h":64, "t":24.5, "temperature":24.5, ...}
{"count":16986, "total":16986}
```

### EMQX에서 실제로 받은 메시지 예시

public EMQX 브로커(`broker.emqx.io`)의 `sensor/data` 토픽을 구독했을 때 Spring Boot 콘솔에 이런 메시지가 들어왔음:

```text
📨 MQTT 수신: {"count":16986,"total":16986}
📨 MQTT 수신: {"h":64,"humidity":64,"slaveAddr":1,"t":24.5,"temperature":24.5,"timestamp":"2026-05-27 13:23:18.351"}
📨 MQTT 수신: {"h":65,"humidity":65,"slaveAddr":1,"t":25,"temperature":25,"timestamp":"2026-05-27 13:23:20.502"}
📨 MQTT 수신: {
  "msg": "hello3"
}
```

의미:

| 필드 | 의미 |
|------|------|
| `temperature`, `t` | 온도 |
| `humidity`, `h` | 습도 |
| `slaveAddr` | 센서/디바이스 주소 |
| `timestamp` | 측정 시간 |
| `count`, `total` | 누군가의 카운터 테스트 데이터 |
| `msg` | 테스트 메시지 |

이 데이터는 우리가 만든 게 아니라 **public 브로커의 같은 토픽을 쓰던 다른 사용자의 메시지**다.

### EMQX 대시보드 상태 출력 예시

EMQX Dashboard에서 Node 상태를 보면 다음처럼 나옴:

```text
Name              emqx@172.20.0.2
Status            Running
Uptime            29 minutes 36 seconds
Version           6.0.0
Connections       0
Erlang Processes  2107
OS Memory         1.37G
OS CPU Load       0.64/1.03/1.03
```

의미:

| 항목 | 의미 |
|------|------|
| `emqx@172.20.0.2` | Docker 내부 EMQX 노드 이름/IP |
| `Running` | 브로커 정상 실행 중 |
| `Uptime` | 실행된 시간 |
| `Version` | EMQX 버전 |
| `Connections` | 현재 연결된 MQTT 클라이언트 수 |
| `Erlang Processes` | EMQX 내부 Erlang 프로세스 수 |
| `OS Memory` | 컨테이너 메모리 사용량 |
| `OS CPU Load` | CPU 부하 평균 |

### 배운 점
- 토픽은 누구나 Pub/Sub 가능한 공유 자원
- 공용 브로커는 네이밍 충돌 위험
- 실서비스는 반드시 내 브로커 + 인증 필요

### 테스트 도구
- `mosquitto_pub -h localhost -t sensor/data -m "hello"`
- EMQX 대시보드 WebSocket Client (불안정)
- MQTTX GUI (설치했으나 충돌 이슈)

---

## Step 9: MQTT 헤더 확인

Subscriber에서 `message.getHeaders()` 로깅:
- `mqtt_receivedTopic` — 어느 토픽으로 왔는지
- `mqtt_receivedQos` — QoS 레벨
- `mqtt_duplicate` — 중복 여부
- `mqtt_receivedRetained` — 유지 메시지 여부
- `timestamp`, `id`

```java
message.getHeaders().forEach((k, v) ->
    System.out.println("   Header: " + k + " = " + v));
```

---

## Step 10: EMQX 라이선스

### 대시보드 안내문

> You are running EMQX under the Community License (single-node).
> EMQX를 커뮤니티 라이선스(단일 노드)에 따라 실행하고 있습니다.
>
> **Current Permissions (Free Use):** — 현재 사용 권한 (자유 사용):
> - All non-production activities.
>   - 모든 비생산 활동.
> - Single-node production deployment: Provided you are not offering EMQX commercially as a hosted service or embedding it into a product you sell.
>   - 단일 노드 프로덕션 배포: EMQX를 호스팅 서비스로 상업적으로 제공하거나 판매하는 제품에 포함시키지 않는 경우.
>
> **Commercial License Required For:** — 상업용 라이선스가 필요한 경우:
> - Cluster deployments (2+ nodes)
>   - 클러스터 배포 (2개 이상의 노드)
> - Offering EMQX as a commercial SaaS or hosted service
>   - EMQX를 상용 SaaS 또는 호스팅 서비스로 제공
> - Embedding/distributing EMQX within a commercial product that you sell
>   - 판매하는 상용 제품에 EMQX를 포함시키거나 배포

### 1노드 vs Cluster

```
1노드 (지금)
┌──────┐
│EMQX 1│  ← 단일 서버
└──────┘

2노드 클러스터
┌──────┐  ┌──────┐
│EMQX 1│──│EMQX 2│  ← 한쪽 죽어도 계속 동작
└──────┘  └──────┘
```

### 노드 수 선택 기준

| 상황 | 1노드 | 2노드+ 클러스터 |
|------|-------|----------------|
| 디바이스 100대 | ✅ 충분 | 오버스펙 |
| 디바이스 10만대 | ❌ 터짐 | ✅ 분산 처리 |
| 서버 다운 | ❌ 서비스 중단 | ✅ 자동 페일오버 |
| 점검/업데이트 | ❌ 중단 필요 | ✅ 무중단 가능 |

> **공부하는 지금은 1노드 무료로 충분하다.**

### EMQX 버전별 가격

| 버전 | 가격 | 용도 |
|------|------|------|
| **EMQX Open Source** (Docker) | **무료** | 개인 학습, 소규모 개발 |
| **EMQX Enterprise** | 유료 | OAuth, Kafka 연동, 클러스터 모니터링 |
| **EMQX Cloud** | 사용량 과금 | AWS/GCP에托管된 서비스 |

---

## 핵심 개념 정리

| 개념 | 설명 |
|------|------|
| **Broker** | 메시지 중계 서버 (EMQX, Mosquitto) |
| **Client** | 발행/구독 주체 (Spring Boot, IoT 디바이스) |
| **Topic** | 메시지 주소. 계층 구조 (`a/b/c`), 와일드카드 (`+`, `#`) |
| **QoS** | 메시지 보장 레벨 (0=최대1회, 1=최소1회, 2=정확히1회) |
| **Pub/Sub** | 발행자와 구독자가 직접 연결되지 않고 브로커가 중계 |

### QoS 상세

| QoS | 이름 | 의미 | 비유 |
|-----|------|------|------|
| **0** | At most once | 보내고 끝. 확인 안 함. 유실 가능 | 편지를 우체통에 넣고 끝 |
| **1** | At least once | 받았는지 확인. 중복 가능하지만 꼭 도착 | 등기. 꼭 보내지는데 여러 번 올 수도 |
| **2** | Exactly once | 정확히 한 번. 핸드셰이크 2회 | 내용증명. 중복 없이 정확히 |

**선택 기준**

```
QoS 0: 잃어도 되는 데이터
  → 온도, 습도, GPS (다음 값으로 갈음)

QoS 1: 한 번은 받아야 하는 데이터
  → 대부분의 센서 데이터 (실전 기본값)

QoS 2: 절대 잃거나 중복되면 안 되는 데이터
  → 도어락 잠금, 알람, 결제 명령
  → 트래픽 2배라 성능 부담 있음
```

> 실전에서는 **QoS 1**이 가장 많이 쓰인다.

### MQTT vs Raw Socket

| | Socket | MQTT |
|------|--------|------|
| 연결 | 1:1 (IP/Port) | N:N (브로커 경유) |
| 주소 체계 | 없음 | 토픽 |
| 재연결 | 직접 구현 | 자동 |
| QoS | 직접 구현 | 레벨 선택 |
| 확장 | 중계 서버 직접 제작 | 브로커가 라우팅 |

### 보안
- 기본 MQTT는 인증 없음 → 공용 브로커에서 누구나 Pub/Sub 가능
- 실서비스: Username/Password, TLS, ACL 필수
- EMQX에서 인증/ACL 통합 관리 가능

### 프로젝트 구조

| 항목 | 값 |
|------|-----|
| 프로젝트명 | `MQTT_EMQX` |
| Broker | EMQX (`:1883`) + Mosquitto (`:1884`) 병행 사용 가능 |
| Docker | `compose.yaml`에 두 브로커 모두 등록 |

#### 브로커 전환

`application.yml`에서 broker 주소만 바꾸면 바로 전환:
```yaml
# EMQX
broker: tcp://localhost:1883

# Mosquitto
broker: tcp://localhost:1884
```

> Mosquitto는 완전 무료(EPL 2.0), 라이선스 제한 없음.
> 대시보드는 없지만 CLI(`mosquitto_sub/pub`)로 충분히 테스트 가능.

---

## Step 11: Mosquitto + MQTTX/CLI 테스트

### Mosquitto 실행 상태

현재 `compose.yaml`에서 Mosquitto는 로컬 포트 `1884`로 열어둠.

```yaml
mosquitto:
  image: eclipse-mosquitto:latest
  ports:
    - "1884:1883"
```

의미:

| 위치 | 포트 |
|------|------|
| 내 컴퓨터 | `1884` |
| Docker 컨테이너 내부 Mosquitto | `1883` |

즉 외부 클라이언트(MQTTX, Spring Boot, CLI)는 `localhost:1884`로 접속해야 함.

### MQTTX에서 Mosquitto 접속

| 항목 | 값 |
|------|-----|
| Host | `localhost` |
| Port | `1884` |
| SSL/TLS | OFF |
| Username/Password | 인증 안 쓸 때는 비움 |

주의:

```text
broker.emqx.io + 1884  → 틀림
localhost + 1884       → Mosquitto 연결
broker.emqx.io + 1883  → public EMQX 연결
```

### Spring Boot에서 Mosquitto 접속

`application.yml`:

```yaml
mqtt:
  broker: tcp://localhost:1884
  client-id: spring-boot-client
  default-topic: sensor/data
  qos: 0
```

주의:

```text
mqtt://  → MQTTX 앱에서는 보이지만 Java Paho에서는 안 됨
 tcp://  → Spring Boot / Paho에서 사용해야 하는 형식
```

### mosquitto_sub는 대기 상태가 정상

```bash
mosquitto_sub -h localhost -p 1884 -t sensor/data -v
```

이 명령어는 메시지가 올 때까지 계속 기다림.
아무 반응이 없는 것은 오류가 아니라 **구독 대기 중**이라는 뜻.

### 발행해야 구독 화면에 찍힘

터미널 1:

```bash
mosquitto_sub -h localhost -p 1884 -t sensor/data -v
```

터미널 2:

```bash
mosquitto_pub -h localhost -p 1884 -t sensor/data -m "hello_mosquitto"
```

터미널 1에 이렇게 찍히면 성공:

```bash
sensor/data hello_mosquitto
```

### Mosquitto 인증 실험

처음에는 Username/Password를 설정했지만 MQTTX GUI에서 재연결 문제가 생김.
CLI에서는 인증 성공:

```bash
mosquitto_pub -h localhost -p 1884 -u woong -P 1234 -t test -m "hello"
```

하지만 MQTTX는 내부적으로 연결을 다시 만들면서 인증/재연결이 꼬일 수 있음.
공부 단계에서는 일단 익명 접속으로 단순화:

```conf
listener 1883
allow_anonymous true
```

### 정리

| 도구 | 역할 |
|------|------|
| Mosquitto | 브로커 |
| MQTTX | GUI 클라이언트 |
| mosquitto_sub | CLI 구독 클라이언트 |
| mosquitto_pub | CLI 발행 클라이언트 |

Mosquitto는 브로커라서 혼자 메시지를 만들지 않는다.
구독자가 대기하고 있어도 발행자가 메시지를 보내야 화면에 출력된다.

### mosquitto_sub에서 헤더/메타값 보기

기본 `-v` 출력:

```bash
mosquitto_sub -h localhost -p 1884 -t sensor/data -v
```

출력:

```text
sensor/data hello2
```

의미:

| 부분 | 의미 |
|------|------|
| `sensor/data` | topic |
| `hello2` | payload |

디버그 모드:

```bash
mosquitto_sub -h localhost -p 1884 -t sensor/data -v -d
```

출력 예시:

```text
Client null received PUBLISH (d0, q0, r0, m0, 'sensor/data', ... (6 bytes))
sensor/data hello2
```

값으로 풀면:

| 축약 | 의미 | 값 |
|------|------|-----|
| `d0` | duplicate | `false` |
| `q0` | QoS | `0` |
| `r0` | retained | `false` |
| `m0` | message id | `0` |
| `'sensor/data'` | topic | `sensor/data` |
| `6 bytes` | payload size | 6바이트 |
| `hello2` | payload | `hello2` |

Spring Boot에서 보이는 `id`, `timestamp`는 MQTT 원본 헤더가 아니라 **Spring Message가 내부적으로 추가한 메타데이터**다.

### mosquitto_sub 출력 포맷 지정

MQTT 메타값을 직접 값처럼 보고 싶으면 `-F` 옵션 사용:

```bash
mosquitto_sub -h localhost -p 1884 -t sensor/data \
  -F 'topic=%t qos=%q retain=%r messageId=%m payload=%p'
```

출력:

```text
topic=sensor/data qos=0 retain=0 messageId=0 payload=hello2
```

JSON 형태로 보기:

```bash
mosquitto_sub -h localhost -p 1884 -t sensor/data -F '%j'
```

출력 예시:

```json
{"tst":"2026-05-27T15:30:00+0900","topic":"sensor/data","qos":0,"retain":0,"payload":"hello2"}
```

### Payload 안의 JSON 값 보기

MQTT 메시지의 진짜 데이터는 대부분 payload 안에 JSON으로 들어간다.

발행:

```bash
mosquitto_pub -h localhost -p 1884 -t sensor/data \
  -m '{"temperature":24.6,"humidity":62.7}'
```

구독 후 JSON 예쁘게 출력:

```bash
mosquitto_sub -h localhost -p 1884 -t sensor/data -F '%p' | jq
```

특정 값만 보기:

```bash
mosquitto_sub -h localhost -p 1884 -t sensor/data -F '%p' | jq '.temperature'
```

출력:

```text
24.6
```

정리:

| 보고 싶은 것 | 방법 |
|--------------|------|
| topic/qos/retain/messageId | `mosquitto_sub -F` |
| duplicate 등 패킷 디버그 | `mosquitto_sub -d` |
| payload 전체 | `%p` |
| payload 안 JSON 값 | `%p | jq` |
